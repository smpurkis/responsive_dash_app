center_style = {
    "text-align": "center",
    "padding": "1em",
    "max-width": "100%",
    "margin": "0 auto"
}

div_style = {
    "display": "flex",
    "flex-direction": "column",
}

button_style = {
    "text-align": "center",
    "max-width": "20rem",
    "margin": "0 auto",
    "margin-top": "1em",
}

dropdown_style = {
    "text-align": "center",
    "max-width": "20rem",
    "margin": "0 auto",
    "min-width": "12rem"
}

h2_style = {
    "color": "#ff3e00",
    "text-transform": "uppercase",
    "font-size": "4em",
    "font-weight": "100"
}

img_style = {
    "text-align": "center",
    "min-width": "200px",
    "width": "75%",
    "max-width": "600px",
    "margin": "0 auto",
    "border": "5px solid #555",
}
